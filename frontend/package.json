{
  "name": "cah-frontend",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "bower update && pub install",
    "serve": "pub serve"
  },
  "devDependencies": {
    "autoprefixer": "^3.1.0",
    "bower": "^1.3.12"
  }
}
