{
  "name": "cah-frontend",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "bower update && pub install"
  },
  "devDependencies": {
    "bower": "^1.3.12"
  }
}
